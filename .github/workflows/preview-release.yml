name: Preview Release

on:
  push:
    branches: [main]
  pull_request: # run on PRs against any base branch, not just main

permissions: {}

jobs:
  publish:
    # Skip for forks — they don't have write access needed by pkg-pr-new.
    if: github.repository_owner == 'cloudflare' || github.repository_owner == 'streamloop'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - run: vp run build
      - run: vp dlx pkg-pr-new@0.0.66 publish --pnpm './packages/vinext' './packages/cloudflare' './packages/create-vinext-app'
